% F2 - notebook roadmap (top of notebook). Orients the learner:
% a 4-stage left-to-right pipeline (define -> train -> evaluate -> bonus),
% the bonus stage drawn dashed to signal it is optional.
\documentclass[border=2pt]{standalone}
\usepackage{tikz}
\input{style.tex}

\begin{document}
\begin{tikzpicture}[x=1cm, y=1cm,
    % a roadmap box = the shared `block` style, sized to hold a title + subtitle
    stage/.style={block, minimum width=33mm, minimum height=15mm, align=center}]

  % --- the four stages, left to right ---
  \node[stage] (s1) {\textbf{1 $\cdot$ Define SNNs}\\[2pt]
                     {\footnotesize the LIF neuron \& its equations}};
  \node[stage, right=10mm of s1] (s2) {\textbf{2 $\cdot$ Train SNNs}\\[2pt]
                     {\footnotesize surrogate-gradient backprop}};
  \node[stage, right=10mm of s2] (s3) {\textbf{3 $\cdot$ Evaluate SNNs}\\[2pt]
                     {\footnotesize sparsity \& MAC vs AC efficiency}};
  % bonus stage: dashed outline = optional
  \node[stage, dashed, right=10mm of s3] (s4) {\textbf{Bonus $\cdot$ Go faster}\\[2pt]
                     {\footnotesize \texttt{torch.compile} speed-up}};

  % --- arrows between the stages (forward flow) ---
  \draw[synapse] (s1.east) -- (s2.west);
  \draw[synapse] (s2.east) -- (s3.west);
  \draw[synapse] (s3.east) -- (s4.west);
\end{tikzpicture}
\end{document}
